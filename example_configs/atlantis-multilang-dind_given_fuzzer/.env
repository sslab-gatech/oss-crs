POSTGRES_PASSWORD=crs-multilang-postgres
POSTGRES_PORT=5432

# LiteLLM configuration (dummy values for given_fuzzer mode without LLM)
# Use --external-litellm flag to skip internal LiteLLM service
LITELLM_MASTER_KEY=sk-1234
LITELLM_URL=http://dummy:4000
LITELLM_KEY=sk-dummy

# CRS-multilang DinD cache directory (REQUIRED for DinD mode)
# Point to the cache prepared by: ./oss-crs-dind/prepare-cache.sh
# Must be an absolute path on the host machine
# NOTE: Use HOST_CACHE_DIR (not CRS_CACHE_DIR) to avoid env var conflict
HOST_CACHE_DIR=/home/dongkwan/crs-multilang-public/oss-crs-dind/cache/images

# Skip saving results (povs, corpus, workdir) to /artifacts/
# Set to "True" to disable saving results (default: save results)
#CRS_SKIP_SAVE=True

# Input generators for fuzzing (comma-separated)
# Available options:
#   - given_fuzzer: Use provided seed corpus (default)
#   - concolic_input_gen: Concolic execution for input generation
#   - testlang_input_gen: Test language-based input generation
#   - dict_input_gen: Dictionary-based input generation
#   - mlla: Multi-language LLM agent for input generation
# Example: CRS_INPUT_GENS=given_fuzzer,mlla,dict_input_gen
CRS_INPUT_GENS=given_fuzzer
