crs-multilang:
  models: []
  dependencies:
    # Uses host docker socket directly (no dind)
    # Parent image is already on host daemon, no tarball save/load needed
    - host_docker_builder
  modes:
    - full
    - delta
